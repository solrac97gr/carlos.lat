# Sanity Configuration
# Project ID is PUBLIC - safe to commit in code
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2025-12-09

# Write Token is PRIVATE - never commit this!
# Only needed for preview mode or server-side writes
# SANITY_API_WRITE_TOKEN=your-write-token-here

# Preview Token (optional)
# For accessing draft content in preview mode
# SANITY_PREVIEW_SECRET=some-random-string
